# TODO

## Loading logo on website open
- [ ] Add a full-screen splash/loading screen component that uses the existing `frontend/public/images/kabossinc logo.jpg`.
- [ ] Ensure splash shows immediately on app start (before routes render).
- [ ] Hide splash after initial render (or after a short timeout) so the site becomes interactive.
- [x] Add required CSS for the splash screen and logo sizing.
- [x] Update `frontend/src/App.tsx` to render the splash wrapper.
- [x] Run `npm test`/`npm run build` (or `npm run dev`) to verify compilation.


